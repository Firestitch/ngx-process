import { BaseAction } from './base-action';
import { DrawerData } from '../classes/drawer-data';
export declare class MenuAction extends BaseAction {
    actions: MenuAction[];
    private _label;
    private readonly _isGroup;
    constructor(data?: any);
    get isGroup(): boolean;
    get label(): string;
    updateRouterLink(data: any): void;
    checkVisibility(data: DrawerData): void;
}
