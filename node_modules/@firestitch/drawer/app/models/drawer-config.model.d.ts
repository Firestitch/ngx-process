import { Action } from './action.model';
import { IDrawerConfig, IDrawerWidthConfig, IFsDrawerPersistance } from '../interfaces/drawer-config.interface';
export declare class DrawerConfig {
    disableClose: boolean;
    position: 'right' | 'left';
    activeAction: string;
    resizable: boolean;
    width: IDrawerWidthConfig;
    actions: Action[] | null;
    persist: IFsDrawerPersistance;
    constructor(data?: IDrawerConfig);
}
